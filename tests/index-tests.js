var _       = require('lodash'),
    chai    = require('chai'),
    expect  = chai.expect,
    assert  = chai.assert

var consumer = require('../');

describe('Index', function() {

  it('should require dependencies properly', function(done) {
    done();
  });

});
